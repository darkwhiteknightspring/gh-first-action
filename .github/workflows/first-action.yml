name: First Workflow 
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello this is the first workflow"
          echo "Running multiple echo at once"
      - name: Print goodbye
        run: echo "Done - bye !!"
